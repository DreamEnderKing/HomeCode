@page "/gameSnake"
@inject IJSRuntime JSRuntime;
@inject Canvas2D Canvas;

@code
{
    string Msg = "No error.";
    protected override async Task OnInitializedAsync()
    {
        Canvas.Init(JSRuntime);
    }

    public void Draw()
    {
        try {
            Brush l = new LinearGradientBrush((0, 0), (100, 100), (0, (0, 0, 0)), (0.5, (255, 0, 0)), (1, (0, 255, 255)));
            Canvas.DrawRect((0, 0), (100, 100), l);
            Canvas.FillRect((100, 0), (200, 100), l);
        }
        catch(Exception e) {
            Msg = e.Message;
        }
    }
}

<h1>
@Msg
</h1>
<button @onclick="Draw">Draw</button>
<canvas id="canvas" width="400" height="400" />